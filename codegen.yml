overwrite: true
schema:
  - "src/server/schemas/generatedSchema.ts":
      noRequire: true
# documents: "src/server/queries.ts"
generates:
  ./graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
